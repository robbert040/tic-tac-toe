import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
    borderRadius: 20,
    borderColor: 'black',
    borderWidth: 4,
    marginTop: 15,
    marginHorizontal: '10%',
  },
  text: {
    textAlign: 'center',
    fontSize: 24,
    fontWeight: 'bold',
  },
  containerPressed: {
    padding: 20,
    borderRadius: 20,
    borderColor: 'black',
    borderWidth: 4,
    marginTop: 15,
    marginHorizontal: '10%',
    opacity: 0.4,
  },
});

export default styles;
